
#Область ПрограммныйИнтерфейс

Процедура Обновить(Стадии) Экспорт
	
	ЭтотОбъект.Записать(Истина);
	
	Для Каждого Спринт Из Стадии Цикл
		НоваяЗапись = ЭтотОбъект.Добавить();
		НоваяЗапись["id"] = Спринт["id"];
		НоваяЗапись["name"] = Спринт["name"];
	КонецЦикла;
	
	ЭтотОбъект.Записать(Истина);
	
КонецПроцедуры 

#КонецОбласти 