
Процедура Обновить(Спринты) Экспорт
	
	ЭтотОбъект.Записать(Истина);
	ЭтотОбъект.Прочитать();
	
	Для Каждого Спринт Из Спринты Цикл
		НоваяЗапись = ЭтотОбъект.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяЗапись, Спринт);
	КонецЦикла;
	
	ЭтотОбъект.Записать(Истина);
	
КонецПроцедуры