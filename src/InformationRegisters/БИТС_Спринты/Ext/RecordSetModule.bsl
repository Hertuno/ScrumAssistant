 
#Область ПрограммныйИнтерфейс

Процедура Обновить(Знач Спринты) Экспорт
	
	ЭтотОбъект.Записать(Истина);
	
	Для Каждого Спринт Из Спринты Цикл
		НоваяЗапись = ЭтотОбъект.Добавить();
		НоваяЗапись["id"]			= Спринт["id"];
		НоваяЗапись["groupId"]		= Спринт["groupId"];
		НоваяЗапись["status"]		= Спринт["status"];
		НоваяЗапись["name"]			= Спринт["name"];
		НоваяЗапись["dateStart"]	= Спринт["dateStart"];
		НоваяЗапись["dateEnd"]		= Спринт["dateEnd"];
		НоваяЗапись["goal"]			= Спринт["goal"];
	КонецЦикла;
	
	ЭтотОбъект.Записать(Истина);
	
КонецПроцедуры 

#КонецОбласти
